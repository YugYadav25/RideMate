PORT=5001
MONGO_URI=mongodb://localhost:27017/ridemate
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Geocoding Provider Configuration
# Options: nominatim (free, no API key), google, mapbox
GEOCODING_PROVIDER=nominatim

# Google Maps API (optional - only if using google provider)
# Get your API key from: https://console.cloud.google.com/
GOOGLE_MAPS_API_KEY=

# Mapbox Access Token (optional - only if using mapbox provider)
# Get your access token from: https://account.mapbox.com/
MAPBOX_ACCESS_TOKEN=

# LocationIQ API Key (optional - only if using locationiq provider)
# Get your API key from: https://locationiq.com/
LOCATIONIQ_API_KEY=
