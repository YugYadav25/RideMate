RideMate/
├── Frontend (React + TypeScript)
│   ├── 15 screens (Landing, Login, Dashboard, etc.)
│   ├── 30+ components (Chatbot, Maps, Pickers, etc.)
│   ├── Services (API clients, Socket.IO)
│   └── Utils (PDF, calculations, weather, AI)
│
└── Backend (Node.js + Express)
    ├── 5 Models (User, Ride, Booking, Vehicle, Notification)
    ├── 6 Controllers (Auth, Rides, Bookings, etc.)
    ├── 7 Route files (RESTful API endpoints)
    └── Utils (Distance, Geocoding, Cron jobs)